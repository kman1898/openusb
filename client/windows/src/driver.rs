// usbip-win2 driver management for Windows.
//
// Responsibilities:
// - Detect if usbip-win2 driver is installed
// - Install the bundled driver (requires admin elevation)
// - Report driver version and status
//
// The usbip-win2 driver is bundled in client/windows/driver/usbip-win2/
// and installed via the Windows device installer APIs.
//
// Implementation deferred to Windows-specific build.
