// User script execution — runs shell scripts in response to events.
// This functionality is implemented in hooks.rs via run_script().
// Scripts receive event data via OPENUSB_* environment variables.
